# Checklist — Daily Task Tracker

A clean, minimal to-do list web app built with vanilla HTML, CSS, and JavaScript.

## Features
- Add tasks
- Mark tasks as complete
- Delete individual tasks
- Clear all completed tasks at once
- Live task count ("X of Y done")

## Tech Stack
- HTML5
- CSS3 (custom properties, flexbox, animations)
- Vanilla JavaScript (no frameworks or libraries)

## Running it locally
1. Clone this repo
2. Open `index.html` in your browser

No build step, no dependencies — just open and use.

## Planned improvements
- [ ] Persist tasks using `localStorage` so they survive a page refresh
- [ ] Add due dates / priority tags
- [ ] Drag-and-drop task reordering

## Screenshot
*(Add a screenshot here once you push to GitHub — drag an image into the README on GitHub.com and it'll generate the markdown for you.)*